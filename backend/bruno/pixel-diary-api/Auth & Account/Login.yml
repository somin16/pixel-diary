info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "user_email": "",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  user_email: 로그인을 하려는 사용자 이메일
  password: 로그인을 하려는 사용자 비밀번호
